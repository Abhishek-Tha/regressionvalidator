{
  "name": "blockguard-mcp",
  "version": "0.1.0",
  "description": "BlockGuard MCP server — conversational EDS block regression tools",
  "type": "module",
  "main": "dist/server.js",
  "bin": {
    "blockguard-mcp": "dist/server.js"
  },
  "scripts": {
    "build": "tsc --build",
    "start": "node dist/server.js",
    "clean": "rimraf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@blockguard/core": "*",
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.4.0"
  }
}
